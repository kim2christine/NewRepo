//Just like a Java Model Class, our React Interfaces typically MODEL some data/data type we intend to use
export interface UserInterface {
  userId?: number;
  username: string;
  password?: string;
  role?: string;
  //? because there's scenarios where we won't need them
}

//THIS FILE IS DONE DON'T TOUCH
